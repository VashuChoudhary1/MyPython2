# tuples works as similar to list but we can't assign new value in it
# tuple is immutable
tup1 = (1,2,3,4,5)
# for single value tupple
tup = ("abc",) # comma should mentioned otherwise it is read as string


print(tup)
print(type(tup))

print(len(tup))

#slicing
print(tup1[1:3])